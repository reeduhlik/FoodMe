enum PersonalMenuState { home, add, profile, impact }
enum ProviderMenuState { home, add, profile, impact }
enum BusinessMenuState {add, profile }
enum PersonalListingsMenuState {map, list}
enum ProviderListingsMenuState {map, list}
